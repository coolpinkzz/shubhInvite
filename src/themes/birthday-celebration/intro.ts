import type { ThemeIntroConfig } from "@/types/theme";

/** Tap overlay — no video; unlocks music and reveals the invitation. */
export const birthdayCelebrationIntro = {
  emblemSrc: "/themes/birthday-celebration/spiderman.png",
  skipOnReducedMotion: false,
} satisfies ThemeIntroConfig;
